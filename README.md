# test-mappa
L'obiettivo di questo test è di creare un'interfaccia web in cui viene mostrata la planimetria di alcune stanze e creare dei bottoni al cui click le stanze vengono colorate (di colori differenti)
